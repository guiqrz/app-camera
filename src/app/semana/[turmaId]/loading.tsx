/**
 * Esqueleto de `/semana/{id}` — o mesmo de `/semana`.
 *
 * As duas rotas desenham a MESMA tela, so' que uma filtrada por turma.
 * Reexportar mantem os dois lados da transicao identicos, sem duplicar markup;
 * mesmo padrao de /relatorios e /chamada.
 */
export { default } from "../loading";
