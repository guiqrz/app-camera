/**
 * Esqueleto de uma conversa do Cup AI enquanto ela e' carregada.
 *
 * Reexporta o esqueleto da lista de conversas (22/08/2026): as duas telas tem
 * o mesmo formato — o mesmo AppShell e a mesma coluna central — entao reusar
 * mantem a transicao entre elas sem piscada e sem markup duplicado.
 */
export { default } from "../loading";
